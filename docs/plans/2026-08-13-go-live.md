# Go-live — 13 Aug 2026

Canonical: **https://www.sahikaoncuminikler.com**  
Proof time: 2026-08-13 ~19:30 UTC. Live already includes security headers + temsili gallery. **Repo remediations are complete**; they are not all on prod until you deploy this branch.

**Decision: NO-GO** until this branch is on production. Then: remove Vercel `Access-Control-Allow-Origin: *`, submit Search Console. Residual Next 15 `postcss`/`sharp` npm highs accepted (no `--force` to Next 16).

---

## Live proof

```
HTTP www → https://www.sahikaoncuminikler.com/   308
HTTPS apex → https://www.sahikaoncuminikler.com/ 308
HTTPS www /                         200
HSTS max-age=63072000
CSP + X-Frame-Options: SAMEORIGIN + X-Content-Type-Options: nosniff
Referrer-Policy, Permissions-Policy present
Access-Control-Allow-Origin: *      STILL SET (Vercel, not repo)
robots.txt 200 Allow: / Sitemap: https://www.sahikaoncuminikler.com/sitemap.xml
sitemap.xml 200  eight canonical www URLs
/iletisim.html → /iletisim          308
Pages 200: / /hakkimizda /programlar /beslenme /galeri /sss /iletisim /gizlilik
Unknown URL 404
No x-robots-tag noindex
```

---

## 1. Quality

| Priority | Check | Status |
|----------|-------|--------|
| MUST | Tests pass on the deploy branch | [N/A] no unit tests; gate is lint + tsc + build + verify-gallery (green locally) |
| MUST | CI passes (tests + lint + types) | [x] `.github/workflows/ci.yml` in repo; green after first push |
| MUST | No unresolved CRITICAL/HIGH code findings | [x] FAQ 18–27; `scripts/**` ignored; lint + typecheck + build 0 |
| SHOULD | No launch-related TODO/FIXME | [x] |
| SHOULD | No leftover console.log | [x] |

## 2. Security

| Priority | Check | Status |
|----------|-------|--------|
| MUST | No secrets in source | [x] |
| MUST | No unresolved CRITICAL/HIGH security | [x] photos down; headers live |
| MUST | Deps no known HIGH+ vulns | [ ] 3 residual Next 15 nested (`postcss`/`sharp`); accepted, no Next 16 |
| MUST | HTTPS; HTTP → HTTPS | [x] 308 |
| MUST | No mixed content | [x] assets `/_next` + `/galeri` HTTPS |
| MUST | Live HSTS + nosniff + CSP or XFO | [x] all three |
| MUST* | Auth | [N/A] |
| MUST* | LLM | [N/A] |
| SHOULD | CORS not `*` | [ ] live still `Access-Control-Allow-Origin: *` — **Vercel dashboard** |
| SHOULD | SSL Labs A | [ ] manual |

## 3. Compliance

| Priority | Check | Status |
|----------|-------|--------|
| MUST | Privacy exists, accurate, linked | [x] in repo (Maps, Vercel, child data, retention, generated images); **deploy to prove live** |
| MUST | Copyright in footer | [x] © 2026 + brand name |
| MUST | Third-party assets licensed | [x] generated/illustrative gallery; fonts via next/font |
| MUST* | Cookie banner | [N/A] no marketing cookies |
| MUST* | ToS | [N/A] |
| MUST* | COPPA age gate | [N/A] parents are users |

## 4. Accessibility

| Priority | Check | Status |
|----------|-------|--------|
| MUST | Images alt | [x] decorative empty / temsili alts |
| MUST | Inputs labeled | [x] |
| MUST | Keyboard | [x] skip link `#icerik` + marquee Durdur/Oynat; **deploy to prove live** |
| MUST | Color not only meaning | [x] |
| MUST | Heading order | [x] sr-only h1 then h2s |
| SHOULD | reduced-motion | [x] in CSS |

## 5. Performance

| Priority | Check | Status |
|----------|-------|--------|
| MUST | Key pages usable | [x] static 200s |
| MUST | Images width/height | [x] 1536×1024 |
| SHOULD | LCP/INP/CLS | [ ] re-measure after deploy |

## 6. SEO

| Priority | Check | Status |
|----------|-------|--------|
| MUST | Unique title + description | [x] |
| MUST | Live robots.txt + sitemap | [x] |
| MUST | Canonical host www | [x] apex 308 → www |
| MUST | Not noindex | [x] |
| MUST | Self-canonical HTTPS | [x] |
| SHOULD | OG 1200×630 | [x] `src/app/opengraph-image.tsx` 1200×630; **deploy to prove live** |
| SHOULD | JSON-LD | [x] ChildCare |
| SHOULD | Favicons | [x] |
| SHOULD | Useful 404 | [x] branded `not-found.tsx`; **deploy to prove live** |

## 7. Ops

| Priority | Check | Status |
|----------|-------|--------|
| MUST | Deploy process tested | [x] prod is serving Vercel |
| MUST | Rollback < 5 min | [x] README: Vercel Instant Rollback; not drill-tested |
| MUST | Prod env | [x] no staging secrets |
| MUST | Backups | [N/A] static / git |
| MUST* | Email SPF | [N/A] no transactional mail |
| SHOULD | TLS auto-renew | [x] Vercel |

## 8. Open-source

[N/A] private

## 9. Live / DNS / indexing

| Priority | Check | Status |
|----------|-------|--------|
| MUST | Hitting production | [x] www + Vercel |
| MUST | Apex/www resolve; non-canonical 301s | [x] 308 |
| MUST | HTTP → HTTPS | [x] 308 |
| MUST | Certificate covers names | [x] HTTPS 200/308 |
| MUST | DNS propagated | [x] |
| MUST | noindex only on non-prod | [x] prod index,follow |
| MUST | No staging URLs in HTML | [x] siteConfig.url is www prod |
| MUST | Analytics fires | [N/A] none installed |
| SHOULD | Old .html redirects | [x] `/iletisim.html` → `/iletisim` |
| SHOULD | Status codes | [x] 200 / 308 / 404 |
| SHOULD | GSC verified + sitemap | [ ] **manual — user action required** |

---

## Open (why still NO-GO)

1. **Deploy this branch** so gizlilik, skip link, 404, OG, FAQ, and CI exist on www.
2. Remove `Access-Control-Allow-Origin: *` in the Vercel dashboard.
3. Google Search Console: verify the www property and submit `sitemap.xml`.
4. Optional: uptime ping, SSL Labs, official unvan.

## After GO

Follow post-launch (first 72h): console, WhatsApp path, PageSpeed, GSC inspect.
